# Commands

Build, test, run, flags, and verification are in [`RunningAndVerifying/`](../RunningAndVerifying/). Shader recompiles: `tools\CompileShaders.ps1` — see [`ShaderWorkflow.md`](../ShaderWorkflow.md).
